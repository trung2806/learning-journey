# 2026-06-24, Day 32 of Phase 1 (Thứ Tư, tuần 4): ritual clone-zero-paste bắt gap journal-vs-repo (commit Day 31 chưa từng push); land re-pin cộng recapture full-fidelity LÊN repo thật; rồi phát hiện re-pin Day 31 mới phủ 4/8 hàm real-capture (bốn hàm pin temp/cpu, `test_pipeline.py` x3 cộng fetcher-unwrap, sống nhờ drift-luck vì temp/cpu không trôi phiên ấy), đóng nốt thành 8/8 drift-proof. Baseline 40 green trên fixture full-fidelity thật.

## STATE (đọc đầu phiên, ghi đè mọi suy đoán từ file mirror)

Khối này là điểm vào mỗi phiên. Đọc trước khi làm gì khác.

- **Repo code PUBLIC: `github.com/trung2806/srl-monitor`** (local `~/srl-monitor`). Journal repo riêng, PUBLIC: `github.com/trung2806/learning-journey`.
- **Mirror KHÔNG phải nguồn-sự-thật.** File code trong Project mirror (`srl_*.py`, `control_A.json`, `test_*.py`) đông cứng Day 27, silent-fail. KHÔNG dùng để suy baseline hay state code. Mirror chỉ tin được cho journal `.md`.
- **Baseline lấy bằng MỘT bước, zero paste:**
  ```bash
  git clone https://github.com/trung2806/srl-monitor /tmp/srl && cd /tmp/srl && git log -1 --oneline && python -m pytest -q
  ```
  Ra số nào, đó là baseline. KHÔNG mở lại màn reconcile mirror-vs-đĩa. **Ritual này không phải nghi thức: Day 32 nó bắt được commit Day 31 mà journal khẳng định có nhưng public repo không có.**
- **Baseline hiện tại: 40 green** trên HEAD Day 32. `control_A.json` đã là capture full-fidelity thật (1469 dòng) thay bản trim 941B (nợ Day 20). Re-pin: mọi test real-capture đều fixture-derived (8/8); synthetic giữ pin cứng.
- **Lineage:** `3667cf9` D28 → `62edd31` D29 → `c712649` D30 → `a3f882c` (Day 32, re-pin 4 hàm đã đỏ) → `e9181fe` (Day 32, recapture full-fidelity) → \<follow-up Day 32: đóng nốt re-pin 8/8, 2 file `test_pipeline.py` cộng `test_srl_fetcher.py`\>. **Lấy hash thật bằng `git log -1 --oneline` sau khi push follow-up, dán vào đây thay marker.**
- **Tự kiểm Day 33:** `grep -rn '== [0-9]' test_pipeline.py test_srl_fetcher.py` mà còn ra pin giá trị real-capture nghĩa là follow-up CHƯA land; phải commit trước khi đếm tiếp.
- **Trạng thái assume: stack KHÔNG còn seam mock-only.** Live netmiko validate end-to-end Day 31; envelope confirm flat trên device. Transport không còn là chân trời.

## DẪN: ritual clone-zero-paste trả công, bắt journal nói có commit mà repo không có

Mở phiên bằng ritual chuẩn. Nó phơi ngay: commit "Day 31 follow-on (recapture cộng re-pin)" mà journal Day 31 khẳng định (dòng 51, 89) KHÔNG nằm trên public repo. HEAD vẫn là `c712649` (Day 30), fixture vẫn trim 941B, 4 pin memory vẫn `== 29`. Không phân biệt được từ repo là chưa commit hay commit local mà chưa push, nhưng kết quả như nhau: public ở trạng thái trước-recapture.

Bài học hạng nặng: một journal khẳng định một commit là GIẢ THUYẾT cho tới khi clone xác nhận. Ritual tồn tại đúng để bắt gap này. Bỏ ritual = tin journal = thừa kế lie. Đây là lần đầu nó trả công cụ thể.

## Phát hiện sâu hơn: re-pin Day 31 mới phủ 4/8 hàm, gap là những hàm pin metric KHÔNG trôi phiên ấy

Khi recapture, CHỈ memory trôi (29 sang 28); cpu giữ 2, temp giữ 50. Nên đúng 4 test pin `memory == 29` gãy: engine `test_parsers`, monitor `test_evaluate_metrics_on_real_capture`, main `test_build_report`, fetcher `test_fetch_then_parse`. Day 31 vá TRỌN 4 cái đó, derive cả ba metric chứ không riêng memory. Tốt cho 4 hàm ấy, không phải làm ẩu.

Nhưng cùng anti-pattern còn sống ở 4 hàm KHÁC pin temp/cpu/margin chứ không pin memory: `test_pipeline.py` x3 (`test_pipeline_on_real_capture`, `test_pipeline_render`, `test_pipeline_unwraps`) cộng `test_srl_fetcher.py::test_fetch_unwraps_single_element_list`. Chúng KHÔNG gãy khi recapture vì temp/cpu không trôi, nên không vào diện đỏ, Day 31 không thấy. Xanh KHÔNG vì đúng mà vì temp cố định 50 trên sim, cpu idle avg-1 = 2, margin 25 không nhúc nhích. Đó là drift-luck.

Đây là gãy then chốt: "vá cái gì gãy" chỉ phơi test pin metric DUY NHẤT trôi phiên ấy (memory). Cả lớp (mọi pin giá trị real-capture) LỚN HƠN "cái đã đỏ", vì test khác pin metric phiên này đứng yên. Suy ra **completeness của một re-pin KHÔNG lái được bằng "pytest đỏ chỗ nào": đỏ chỉ phủ metric trôi.** Phải grep cả lớp.

Chứng minh bằng mutate fixture: đẩy temp/cpu/margin trôi thì đúng 4 hàm này gãy, không hơn không kém. Cách phơi nợ là mutate fixture qua dải realistic CÙNG LÚC (cpu, temp, margin, mem), không đợi thiên nhiên làm trôi một metric.

## Land lên repo cộng recapture full-fidelity (a3f882c, 7d11d89, e9181fe)

Công việc committable của Day 31 cuối cùng đã lên repo:
- `a3f882c`: re-pin 4 HÀM real-capture đã gãy khi recapture (engine `test_parsers`, monitor `test_evaluate_metrics_on_real_capture`, main `test_build_report`, fetcher `test_fetch_then_parse`), derive trọn cả ba metric ở đó. Message ghi "4->8" nhưng thực chất mới phủ 4 hàm; còn 4 hàm pin temp/cpu (pipeline x3, fetcher-unwrap) chưa đụng (xem mục dưới).
- Recapture full-fidelity vào bằng HAI commit trùng message: `7d11d89` (−26 dòng, làm hụt fixture) rồi `e9181fe` (+1469 dòng, full thật). HEAD cuối đúng (full-fidelity, 40 green), nhưng history dư một nhịp.

Thứ tự cứu một bàn: re-pin (fixture-derived) đi TRƯỚC recapture nên fixture full về là no-op trên test. memory thật đã trôi 29 sang 28 trong bản capture mới, nhưng pin memory giờ derive nên không gãy. Đảo thứ tự (recapture trước, re-pin sau) thì recapture đẻ churn test ngay.

Quyết về history: đã push thì KHÔNG rebase công khai để dọn cosmetic. Hai commit recapture trùng message là xấu nhưng vô hại; rủi ro rebase lớn hơn lợi thẩm mỹ. Ghi nhận, để nguyên.

## Đóng nốt 4/8 sang 8/8 (follow-up Day 32)

Hoàn tất re-pin cho 4 hàm còn lại: temp derive từ `temperature.instant`, cpu từ `index'all'.total.average-1`, margin từ `temperature.margin` (cả trong metric dict lẫn trong chuỗi dashboard render dùng `str(margin)`). Bỏ luôn note docstring "(Envelope thật vẫn nợ verify trên device)" đã obsolete từ Day 31.

Validate drift-proof trên chính fixture full-fidelity đang ở repo: mutate cpu 2 sang 9, temp 50 sang 53, margin 25 sang 22, mem 28 sang 31 thì vẫn 40 green; trước khi vá, mutate y hệt làm gãy 4. grep pin giá trị real-capture giờ trống. Synthetic crosswire/range giữ nguyên.

## Ruling

- **Re-pin một lớp phải quét HẾT cả lớp, không dừng ở những test tình cờ đỏ phiên này.** Day 31 vá TRỌN 4 hàm đã đỏ (memory trôi nên chỉ chúng đỏ), không làm ẩu. Nhưng "cái đã đỏ" không phải "cả lớp": 4 hàm pin temp/cpu không đỏ vì metric đứng yên, vẫn cùng bệnh. Bệnh là cả 8. Quét cả lớp = grep toàn bộ value-pin, không chờ thiên nhiên làm trôi từng metric.
- **Drift-luck là nợ vô hình.** Một pin cứng trên giá trị real không trôi phiên này TRÔNG như đúng nhưng cùng con bug. Chỉ mutate fixture (hoặc drift thật) mới phơi ra. Test xanh khác test drift-proof.
- **Ritual clone-zero-paste là cơ chế bắt lie, không phải nghi thức.** Journal khẳng định commit = giả thuyết tới khi clone xác nhận. Lần này bắt commit Day 31 không tồn tại trên public.
- **Thứ tự re-pin-trước-recapture-sau khiến recapture thành no-op.** Re-pin khóa contract-trên-field; sau đó đổ fixture full (giá trị khác) không gãy gì. Đảo thứ tự thì recapture đẻ churn test.
- **Đã push thì không rebase history công khai để dọn cosmetic.** Hai commit recapture trùng message xấu nhưng vô hại; rủi ro rebase lớn hơn lợi.

## Pin (test, theo hậu quả)

- **8 test real-capture giờ derive từ fixture:** parser (`test_parsers_on_real_capture`), evaluate (`test_evaluate_metrics_on_real_capture`), smoke (`test_build_report_runs_on_real_capture`, chỉ canh shape cộng `isinstance int`), fetch+parse (`test_fetch_then_parse_on_REAL_bytes`) cộng unwrap (`test_fetch_unwraps_single_element_list`), pipeline evaluate (`test_pipeline_on_real_capture_bytes_through_evaluate`) cộng render (`test_pipeline_render_reaches_dashboard_string`) cộng unwrap (`test_pipeline_unwraps_single_element_list_envelope`). Quy tắc field: cpu = `index'all'.total.average-1`, memory = `utilization`, temperature = `instant`, margin = `temperature.margin`, margin-trong-text = `str(margin)`. Xanh xuyên drift (verify cpu9/temp53/margin22/mem31 và bản gốc temp50/cpu2/mem28).
- **Synthetic GIỮ pin cứng:** crosswire (`instant=99`/`average-1=10` sang 10), aggregate-pick (sang 5), range (sang 82, sang 105, −1), temperature-valid synthetic (sang 50 từ input tự dựng), alarm-status authority, threshold fallback, error-path (`SRLFetchError` short-circuit). Logic field-selection canh bằng input kiểm soát, vai khác hẳn real-capture.
- **BREACH dựa-ngưỡng giữ nguyên** (`test_evaluate_metrics_flags_breach`, BREACH trong pipeline): dựa mem idle ~28-31 cao hơn ngưỡng demo 25. Dưới 25 thì lật, đúng ngữ nghĩa (không còn gì để BREACH), không phải fragility.
- Live (manual, ngoài suite): `live_check.py` cho dashboard khớp mock qua netmiko `nokia_srl` thật (Day 31).

## Glossary

- **Drift-luck (may-không-trôi).** Pin cứng giá trị real sống một phiên KHÔNG vì đúng mà vì metric đó không nhúc nhích phiên ấy. temp cố định 50 trên sim nên `== 50` xanh mãi trên sim, gãy trên hardware. Phân biệt "xanh" với "drift-proof": chỉ cái sau sống qua mutate.
- **Quét cả lớp khác vá-cái-đã-đỏ.** "pytest đỏ chỗ nào, vá chỗ đó" chỉ phơi test pin metric DUY NHẤT trôi phiên ấy (memory). Cả lớp value-pin real-capture LỚN HƠN diện đỏ, vì test khác pin metric phiên này đứng yên (temp cố định 50, cpu idle). Vá trọn cái đỏ vẫn có thể sót cả nửa lớp. Bài kiểm completeness: grep value-pin còn ra dòng không, KHÔNG phải pytest có xanh không.
- **Mutate-to-prove.** Chứng minh drift-proof bằng cố ý đẩy fixture qua dải realistic (cpu, temp, margin, mem cùng lúc), không đợi live drift. Một metric trôi tự nhiên chỉ test một pin; mutate đồng loạt test cả lớp.
- **Ritual là cơ chế, không phải nghi thức.** clone-zero-paste tồn tại để bắt journal-vs-repo gap. Day 32 nó trả công: bắt commit Day 31 ảo. Bỏ ritual = tin journal = thừa kế lie.

## Open item

- ~~Live netmiko~~ **ĐÓNG** (Day 31): chạy thật end-to-end qua `nokia_srl`, dashboard khớp mock.
- ~~Envelope flat-vs-nested~~ **ĐÓNG** (Day 31): flat, confirm trên device.
- ~~Recapture `control_A.json` full-fidelity~~ **ĐÓNG** (Day 32): full envelope thật qua tee đã thực sự LÊN repo (`e9181fe`), không còn là claim journal.
- ~~Re-pin incomplete (4/8)~~ **ĐÓNG** (Day 32 follow-up): 8/8 fixture-derived, verify drift-proof. **Điều kiện: commit follow-up 2 file đã land** (xem Close-out); nếu chưa thì 4 pin pipeline/fetcher vẫn hardcoded.
- ~~github remote cộng push~~ **ĐÓNG**: repo public, clone-zero-paste là default đầu phiên.
- **Giá trị thật trên hardware (không phải sim).** Container cho structure cộng giá trị sim (temp cố định 50), không cho số hardware thật. Không block; horizon ưu tiên thấp, cần thiết bị thật.
- **Mirror content-check.** Vẫn nợ refresh, đã hạ cấp, không block.
- **(Horizon Phase 2) hướng mở rộng:** polling loop cộng emit alert (giờ mới single fetch+render); `healthz.status` làm metric module-health authority-style (envelope thật có, chưa đọc); externalize threshold (demo 25 vs thật ~28-31); fan-out đa node (fabric) khi feature thật cần, YAGNI tới đó.

## Baseline

Engine pure-parse list-based (cpu list/index=all/average-1, memory utilization, temperature instant không range-check). Alert một-evaluator scalar, strict `>`, type-guard threshold. Monitor alarm-status authoritative cho temperature (`basis` mỗi metric, `margin` in-metric), threshold fallback, NO_THRESHOLD trung thực. Fetcher CLI seam: parse cộng error-path cộng envelope-unwrap. Seam `test_pipeline.py` nối fetcher sang evaluate sang render end-to-end. **Transport: live netmiko VALIDATED end-to-end trên SR Linux container thật qua `nokia_srl`, không còn mock-only.** **Envelope: confirm FLAT trên device.** `control_A.json` = capture full-fidelity thật 1469 dòng (đã LÊN repo `e9181fe`, thay bản trim Day 20). **Mọi test real-capture (8 cái) derive từ fixture, miễn drift; synthetic giữ pin cứng.** Git `3667cf9` (D28) → `62edd31` (D29) → `c712649` (D30) → `a3f882c` (re-pin memory, D32) → `e9181fe` (recapture full, D32) → follow-up D32 (đóng re-pin 8/8). **40 green** trên real shape full-fidelity. Mọi Day N+1 đếm từ đây, baseline bằng MỘT lệnh clone+git+pytest, không đọc mirror.

## Close-out (Day 32)

- **Commit follow-up đóng re-pin 8/8** (chạy trên `~/srl-monitor`, thả 2 file đã sinh vào đè bản cũ):
  ```bash
  cd ~/srl-monitor
  python -m pytest -q                          # phải 40 passed
  git add test_pipeline.py test_srl_fetcher.py
  git commit -m "Day32: finish fixture-derived re-pin (4->8); pipeline + fetcher-unwrap temp/cpu/margin now drift-proof"
  git push
  git log -1 --oneline                         # lấy hash thật, dán vào STATE thay marker
  ```
- Journal này: `2026-06-24-02.md` (Day 32). `00-INDEX.md` đã bump ngày cộng con trỏ nhật ký mới nhất; commit kèm.
- **Quy tắc index đi tới (tự động mỗi phiên):** mỗi nhật ký mới, bump "Cập nhật" và "Nhật ký Infra mới nhất" trong `00-INDEX.md`; KHÔNG chép baseline number vào index (giữ single source ở STATE journal, tránh drift hai chỗ).
- `live_check.py` giữ local/scratch, không commit.
- Mirror content-check vẫn nợ refresh, đã hạ cấp, không block.
