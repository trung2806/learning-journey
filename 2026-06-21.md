# 2026-06-21, Day 29 of Phase 1 (Chủ Nhật, tuần 3): reconcile baseline lật ra git-phantom, git mốc THẬT đầu tiên (3667cf9), main.py lậu sửa 3 bug + test, 30 green trên đĩa Day 28

## DẪN: mở bằng verify baseline (Day 27/28 dặn), và verify lật ra phantom tầng sâu hơn con số test, chính là mốc git Day 28 tự khai đã dựng

Day 29 mở đúng kỷ luật: KHÔNG tin "28 green" của Day 28, verify trước khi forward-work. Project mirror trả 20-green (state Day 27), nghịch journal. Đẩy sang artifact đĩa thật. Hai chuyện cùng lòi ra trong một lượt: (1) code Day 28 THẬT (28 rồi 30 passed trên đĩa), mirror chỉ stale, đúng khả năng a; (2) nhưng `git log` từ `~/srl-monitor` trả "fatal: not a git repository", tức mốc git mà Day 28 đóng ngày bằng nó chưa từng tồn tại. Day 29 thành: biến mốc phantom thành mốc thật, và dọn baseline khỏi một file lậu.

## Reconcile baseline: mirror stale (khả năng a), không phải phantom code

Fact, verify bằng hai đường retrieval khớp nhau cộng pytest đĩa:

- Project mirror (cả file-mount lẫn knowledge-search) = 20-green Day 27: `srl_monitor.py` threshold-only, `alarm-status` chỉ echo vào `metadata` top-level, không `basis`; không có `srl_fetcher.py`.
- Đĩa live = 28 passed, `srl_fetcher.py` PRESENT, monitor HAS `basis`.

Lệch là do mirror chưa sync (connector silent-fail mà chính Day 28 đã pre-document), không phải code trên giấy. Bản 20-green tôi đọc là fact về MIRROR cũ, chưa từng là claim về đĩa. Khả năng a thật như khả năng b; phân định bằng artifact đĩa, không bằng lean.

## Git-phantom: claim trung tâm Day 28 không có trên đĩa

`git log` từ thư mục chứa code và test trả "fatal: not a git repository". Không `.git`. Các claim Day 28 "git mốc đầu tiên", "đóng Day 28 bằng commit 28-green", "có git mốc", "git-canonical, Project là mirror downstream" đều phantom. Tách bạch: CODE Day 28 (alarm-status monitor + fetcher) thật, pytest dán; chỉ MỐC GIT là trên giấy. Đây là phantom-verification cùng họ Day 25 (alert-trên-giấy) nhưng ở TẦNG khác: hạ tầng/tooling, không phải con số test. Phantom đội lốt được cả tầng này.

## Đóng phantom: git init + mốc THẬT 3667cf9

`git init` + `.gitignore` (loại `.venv`/cache) + commit `3667cf9` ghim fileset 28-green. Lần đầu binding record kiểm được bằng `git diff` thật sự tồn tại. Bước "git" trong pending-lock Day 28 giờ mới thật khóa, bằng chạy có artifact, không phải ý chí.

## main.py lậu trong baseline: 3 bug + đóng untested

`git add -A` lôi ra `main.py` đi lậu trong `3667cf9`: untested, không mô tả, kèm theo vì add cả thư mục (không ảnh hưởng count 28 vì không test nào import nó). Đọc ra ba bug so code thật:

1. `from srl_display import render_dashboard` trỏ module KHÔNG tồn tại trên đĩa, lại import xong không dùng. Dòng đó ModuleNotFoundError ngay. Bỏ, render inline.
2. `"cpu": {"total": 95}` là shape ĐÃ RETIRE từ Day 19-20. Engine list-based đòi `cpu` là list, `index="all"`, `total.average-1`; dict này làm `parse_cpu_utilization` nổ `ValueError`. Bỏ dict bịa tay, load `control_A.json` thật.
3. `metrics_report["metadata"]["margin_reported"]` sai kép: Day 28 đã bỏ block `metadata` top-level (promote margin/alarm vào trong từng metric), và key `margin_reported` chưa từng tồn tại ở bản nào. Trên đĩa Day 28 dòng này KeyError. Đọc `margin` từ trong temperature metric bằng `.get()`.

Rewrite: đọc capture thật, render inline (không phụ thuộc module ngoài), `basis`/`margin` đọc phòng thủ bằng `.get()` nên đúng trên cả monitor Day 28 (hiện chúng) lẫn bản cũ (bỏ qua), logic tách vào `build_report`/`render`/`main` có `__main__` guard nên import được mà không chạy. Thêm `test_main.py` smoke test. Verify trên đĩa: 30 passed. Dashboard in `TEMPERATURE: 50°C | OK (alarm-status)`, tức temp OK do vendor verdict lái (basis=alarm-status), KHÔNG do threshold (50<70 thì threshold cũng OK). Thiết kế Day 28 hành xử đúng tới tận màn hình, không chỉ pass test.

## Close-out (áp trong phiên Day 29)

- Commit bản sửa `main.py` + `test_main.py` (commit follow-on sau `3667cf9`).
- Annotate `2026-06-20-02.md`: blockquote `⚠️ CORRECTION` ghi git-phantom đã đóng bằng `3667cf9`, giữ entry không xóa theo pattern Day 16-26.
- Refresh Project mirror, verify bằng content-check (hỏi một chi tiết chỉ có trong bản mới), không tin nhãn "Connected".

Phân tầng verify, đây là lesson chính Day 29: `30 green` + `3667cf9` tồn tại + hành vi `main.py` = verify bằng artifact đã dán (pytest/git log) tôi đọc tận nơi. Ba close-out trên = áp trong phiên theo report chạy; mirror-sync verify bằng content-check của người, phía tôi không tự re-confirm được (view Project có thể đóng băng theo phiên). Ghi đúng hai hạng, không gộp.

## Ruling

- **Mirror stale ≠ phantom code.** Khi snapshot lệch journal, lật repo thật TRƯỚC khi kết tội. Khả năng a (mirror chưa sync) thật ngang khả năng b (trên giấy). Day 29 đúng về a, nhưng cùng lúc b cục bộ đúng cho riêng mốc git.
- **Phantom đội lốt được cả tầng hạ tầng.** Day 28 "có git mốc" sai dù code đúng. Bài học: "verified" mà gộp artifact-thật với user-report là chỗ Day 28 vấp và để git-phantom lọt. Phải tách hai mức.
- **Entry point cũng phải đọc bytes thật + test được.** Dict bịa tay tái phạm cpu-shape đã retire; load capture thật + `__main__` guard đóng cả hai cùng lúc (đúng shape, lại testable).
- **Git mốc là điều kiện của binding record.** Day 28 nói đúng nguyên tắc, chỉ chưa làm. Giờ làm: `3667cf9`. Trước nó "binding" chỉ là ý chí.

## Pin (test, theo hậu quả)

- `main.build_report()` trên `control_A.json`: cpu=2 (list/index=all/average-1), memory=29, temperature=50; đúng ba metric.
- `main.render()` không nổ, in cả ba metric + nhãn status; `basis`/`margin` hiện khi monitor cấp (Day 28), bỏ qua khi không (bản cũ) nhờ `.get()`.
- import `main` không chạy side-effect (đọc file nằm trong `build_report`, không ở module top-level), nên test import được.

## Glossary

- **Khả năng a và b cùng phải kiểm; lean về a không phải xác nhận a.** Day 29 reconcile đúng về a (mirror stale) NHƯNG cùng ngày b cục bộ đúng (git trên giấy). Hai tầng drift độc lập trong một lượt verify.
- **"User confirm" không đồng hạng "artifact đã dán".** Day 28 gộp hai cái dưới một nhãn "verified" và git-phantom lọt. Day 29 tách: cái gì `pytest`/`git log` dán mới là fact; cái gì người báo là user-report, ghi đúng hạng đó.
- **Entry point cũng là code, cũng drift.** `main.py` mang shape retire + key chưa từng có, sống lậu ngoài test net tới khi `git add -A` lôi ra. Mọi `.py` trong repo phải có test hoặc lý do không có.
- **`git diff` là artifact-truth rẻ, giờ thật sự có.** `3667cf9` làm divergence sau này chỉ cách một diff. Trước Day 29 không có gì để diff, nên drift trôi xa được tới ba tầng (journal-vs-repo, shape-regress, scramble).

## Open item

- **Live netmiko chưa chạy lần nào (chân trời, cần device).** `SRLCliFetcher` mới validate tầng parse trên bytes thật qua mock SSH; kết nối SSH thật tới lab vẫn là thứ DUY NHẤT còn assume.
- **Re-capture `control_A.json` full-fidelity (nợ Day 20 đến 29).** Bản hiện trim; đè full output `info from state platform control A` khi pull lại từ device.
- **github remote + push (commit local xong; remote + auth là bước người).** Chưa lock bằng artifact ở phía tôi.
- **Project mirror verify định kỳ bằng content-check, không tin nhãn "Connected".** Connector từng silent-fail; chính nó mở ra Day 29. Đây là caution thường trực, không phải sự cố một lần.

## Baseline (đếm lại, tổng pytest-confirmed trên đĩa Day 28)

Engine pure-parse list-based (cpu list/index=all/average-1, memory utilization, temperature instant không range-check). Alert một-evaluator scalar, strict `>`, type-guard threshold. Monitor alarm-status authoritative cho temperature (`basis` mỗi metric, `margin` in-metric), threshold fallback, NO_THRESHOLD trung thực. Fetcher CLI seam validate trên bytes thật (live SSH chưa). `main.py` entry point đọc capture thật + render dashboard inline, có smoke test. Một real capture làm fixture. Git mốc THẬT `3667cf9`, đã có follow-on commit cho main.py. **30 green trên real shape (28 baseline Day 28 + 2 main), có git mốc kiểm được.** Mọi Day N+1 đếm từ đây.
