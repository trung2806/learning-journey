# 2026-06-22, Day 30 of Phase 1 (Thứ Hai, tuần 4): baseline reconcile KHỚP khả năng a (đĩa 30 green @ `62edd31`, git thật, không phantom), thêm lớp seam end-to-end fetcher → evaluate → render trên bytes thật (`test_pipeline.py`, 10 test, baseline 40), và HẠ mirror khỏi vai nguồn-sự-thật

## STATE (đọc đầu phiên, ghi đè mọi suy đoán từ file mirror)

Khối này là điểm vào mỗi phiên. Đọc trước khi làm gì khác.

- **Mirror KHÔNG phải nguồn-sự-thật.** Các file code trong Project mirror (`srl_*.py`, `control_A.json`, `test_*.py`) đông cứng ở state Day 27 (`srl_monitor.py` threshold-only, không `basis`, không `srl_fetcher.py`). Mirror là connector một chiều repo→Project, "Sync now" tay, silent-fail. Tuyệt đối KHÔNG dùng code mirror để suy ra baseline hay state code. Mirror chỉ tin được cho journal `.md`.
- **Nguồn-sự-thật code = `github.com/trung2806/srl-monitor` (PUBLIC)** + git @ `~/srl-monitor`. Journal/docs ở `github.com/trung2806/learning-journey` (PUBLIC, repo khác, không dùng để verify baseline code).
- **Session-start, một bước, zero paste.** Code repo public nên Claude tự clone trong sandbox, không cần Trung dán:
  ```bash
  cd /tmp && rm -rf srl && git clone https://github.com/trung2806/srl-monitor srl && cd srl && git log -1 --oneline && python -m pytest -q | tail -1
  ```
  Ra số nào, đó là baseline. KHÔNG mở lại màn reconcile mirror-vs-đĩa, KHÔNG đọc code mirror.
- **Baseline đã XÁC NHẬN (clone + pytest Day 30): 40 green @ `c712649`** (`3667cf9` Day28 → `62edd31` Day29 → `c712649` Day30 seam test). Per-file: engine 11, alert 6, monitor 6, fetcher 5, main 2, pipeline 10.

## DẪN: verify baseline lần này KHỚP, reconcile rẻ; cái đắt là connector, không phải kỷ luật

Day 30 mở đúng kỷ luật Day 27/28/29 dặn: không tin con số, verify bằng artifact. Lần này artifact đĩa (git log + pytest do Trung dán) KHỚP nguyên claim Day 29: 30 green @ `62edd31` nằm trên `3667cf9`, tree sạch, `main.py`/`srl_fetcher.py`/`test_main.py` present, monitor có `basis` (alarm-status authority). Reconcile đóng = khả năng a (mirror stale), không phantom round này.

Điểm rút ra khác mọi lần trước: phần đắt KHÔNG phải bước verify. Bước verify đúng là một lệnh. Phần đắt là màn reconcile nhiều lượt vì nó trỏ vào kênh hỏng (Project mirror), nơi tôi đọc file Day 27 rồi tưởng project đang ở Day 27, nghịch journal, rồi bắt Trung dán/chẩn đoán. Day 29 đã chứng minh "lệch" đó chỉ là stale, không phải phantom. Lặp lại nghi thức đó mỗi phiên là lợi ích giảm dần trên chi phí cố định.

## Mirror stale lần thứ N: chẩn đoán đúng nhưng trỏ sai kênh

Phiên này view mirror của tôi VẪN 20-green Day 27 (verify tại chỗ: 11 engine + 6 alert + 3 monitor, monitor không `basis`, không fetcher/main). Trung phải PASTE code đĩa để mở việc. Đây không phải sự cố mới, là cùng connector silent-fail đã mở ra Day 29.

Bất đối xứng đáng ghi: journal `.md` trong mirror là MỚI (đọc được Day 29), nhưng `.py` là CŨ (Day 27). Hai luồng sync khác nhau: `.md` Trung tự nạp, `.py` qua connector flaky. Nên kết luận: mirror tin được cho `.md`, không tin được cho code. Trỏ verification vào code-mirror là trỏ vào thứ vừa flaky vừa không cần thiết khi đã có git.

## Lớp seam: phần MỚI là tới render + ma trận lỗi rộng, KHÔNG phải nối fetch→evaluate (đã có)

**Đính chính so dự thảo đầu ngày (lỗi của Claude, lòi ra khi clone repo):** Tôi từng viết "chưa có gì nối `SRLCliFetcher` vào pipeline, fetcher chỉ test cô lập tầng parse". SAI. `test_srl_fetcher.py` (Day 28, 5 test) ĐÃ có và đã làm `fetch_control() → evaluate_metrics()` trên bytes thật (`test_fetch_then_parse_on_REAL_bytes`: cpu=2/memory=29/temp=50/basis=alarm-status), đã test gửi đúng lệnh, đã test unwrap list-1, đã test raise rỗng + CLI-error. Tôi dựng narrative "gap" trên chỗ TRỐNG của mirror (không thấy file đó) cộng bản tôi tự reconstruct, nên thổi phồng khoảng trống. Cùng họ lỗi mirror-as-truth, lần này về phía tôi.

Khoảng trống THẬT, hẹp hơn, mà `test_pipeline.py` (Day 30, 10 test) đóng:

- **Tới tận chuỗi render.** `test_srl_fetcher` dừng ở `evaluate`. `test_pipeline` đi tiếp `→ render()`, assert chuỗi dashboard (nhãn `(alarm-status)`, dòng `TEMP MARGIN`, `MEMORY BREACH`). Đây là phần net-new chắc chắn.
- **Ma trận lỗi rộng hơn.** `test_srl_fetcher` test 2 input lỗi (rỗng, CLI-error). `test_pipeline` parametrize 6, thêm net-new: list rỗng `[]`, scalar `42`, JSON string. Ba cái này không file nào test trước đó.
- **Nhánh BREACH qua ngưỡng demo.** `test_srl_fetcher` dùng `{cpu:80, memory:80}` nên memory OK; `test_pipeline` dùng `DEFAULT_THRESHOLDS` (memory=25) nên chạm nhánh memory BREACH.

Phần TRÙNG (thừa, nên dọn): gửi-đúng-lệnh, fetch→evaluate-real-bytes (lõi), unwrap-list-1, và 2/6 input lỗi (rỗng, CLI-error) lặp lại `test_srl_fetcher`. Tức ~5-6/10 test pipeline là dup khái niệm. Suite vẫn 40 xanh (khác file/tên hàm nên không đụng), nhưng theo ethos "mỗi test phải xứng chỗ" (Day 29, vụ `main.py` lậu) thì nên trim `test_pipeline` về đúng phần net-new: render-reach + 3 input lỗi mới + assertion BREACH, bỏ phần lặp. Quyết định để mở.

Ghi đúng hạng: lớp này (cả phần cũ lẫn mới) đóng WIRING (output fetcher evaluate-compatible, lỗi short-circuit, render đạt), KHÔNG đóng TRANSPORT. SSH vẫn mock. Live netmiko và envelope thật trên device vẫn assume.

## Chốt quy trình: hạ mirror, baseline từ một lệnh

Thay đổi phương pháp, áp từ Day 31:

1. **Hạ mirror khỏi nguồn-sự-thật.** Code-file mirror là hóa thạch Day 27, không đọc để set baseline. Khối STATE trên đầu entry này (và carry sang `00-INDEX.md`) là chỗ neo baseline + lệnh verify.
2. **Promote git làm artifact rẻ.** Baseline = `git log -1` + `pytest -q`, một lệnh. Kỷ luật chống-phantom giữ nguyên (git+pytest vẫn là artifact-truth, không phải lời khai), chỉ còn tốn một bước thay vì một màn.
3. **Hai endgame để giết paste đầu phiên.** (a) Push lên GitHub PUBLIC → tôi clone tự động trong sandbox, zero paste, đồng thời đóng luôn open item "github remote+push". (b) Repo private → Trung dán một dòng `git log -1 && pytest -q | tail -1`. Private + token tự động là KHÔNG khả thi (tôi không được cầm credential). Chọn (a) nếu chấp nhận public (project học, `control_A.json` là demo, không secret); nếu không thì (b), vẫn rẻ hơn màn reconcile cũ nhiều.

## Close-out (Day 30)

- Commit `test_pipeline.py` (follow-on trên `62edd31`).
- Update Claude memory: mirror code-file stale, không bao giờ set baseline từ nó, verify bằng một lệnh git+pytest. (Đã áp trong phiên.)
- Carry khối STATE lên `00-INDEX.md` để đầu phiên sau đọc được không phụ thuộc entry này.
- Quyết định GitHub public-vs-private (mở; nếu public thì push, đóng open item + bật clone tự động).
- Refresh Project mirror (tùy chọn, đã hạ cấp nên không block). Tiêu chí content-check nếu refresh: file list có `srl_fetcher.py`/`main.py`/`test_main.py`/`test_pipeline.py`, và `grep basis srl_monitor.py` ra dòng alarm-status authority.

## Ruling

- **Reconcile rẻ khi trỏ đúng artifact, đắt khi trỏ kênh hỏng.** Day 30 cùng kỷ luật Day 29, chỉ đổi kênh: git thay mirror. Nghi thức không sai, chỗ chĩa sai.
- **"Validated in isolation" ≠ "validated wired".** Day 28 có fetcher test cô lập nhưng pipeline chưa từng nối qua nó. Một module pass cô lập không bảo chứng nó cắm vào chuỗi đúng. Day 30 đóng wiring.
- **Mock đóng wiring, không đóng transport.** Lặp lại lesson Day 29, giờ nâng thành quy tắc thường trực: mỗi lần báo "fetcher xanh" phải kèm "trên mock" cho tới khi SSH thật chạy.
- **Nguồn-sự-thật phải là kênh pull được rẻ VÀ tin được.** Project connector flaky không đạt. Git đạt; GitHub public đạt và còn cho tôi tự pull. Đây là điều kiện hạ tầng của việc bỏ được nghi thức paste.

## Pin (test, theo hậu quả)

- `test_pipeline`: `fetch(mock REAL_BYTES) → evaluate` cho temperature `{value:50, status:OK, basis:alarm-status, margin:25}`, memory BREACH basis=threshold (29>25), cpu value 2 OK.
- `render(run_pipeline(...))` đạt chuỗi dashboard: chứa `SR LINUX MONITORING DASHBOARD`, `(alarm-status)`, `MEMORY`/`BREACH`, `TEMP MARGIN`/`25`.
- Envelope list-1-phần-tử: `fetch_control` unwrap, metrics y hệt flat dict (temperature 50, cpu 2).
- 6 input lỗi fetch nổ `SRLFetchError` tại `fetch_control`, `run_pipeline` short-circuit, evaluate không chạy.
- `FakeNetmiko.last_command` == `CONTROL_CMD` == `"info from state platform control A | as json"` (seam gửi đúng lệnh đã sinh ra capture, provenance khớp).

## Glossary

- **Hạ tầng-sự-thật quyết định được nghi thức rẻ hay đắt.** Tới Day 29 verification trỏ vào Project mirror, một kênh flaky, nên mỗi phiên thành một màn. Day 30 trỏ vào git: cùng sự nghiêm khắc, một lệnh. Bài học không phải "bớt verify", là "verify vào artifact pull-được-rẻ-và-tin-được".
- **Cô lập-xanh không suy ra wired-xanh.** `srl_fetcher` pass test riêng từ Day 28 vẫn không nói gì về việc output nó cắm vào `evaluate_metrics` có đúng shape không. Khoảng trống đó sống tới Day 30 mới có test bắt. Mọi seam giữa hai module cần một test đi qua chỗ nối, không chỉ test hai đầu.
- **Mock-xanh không phải device-xanh.** `test_pipeline` xanh nói pipeline nối đúng và xử lỗi đúng GIẢ SỬ transport trả bytes như mock. Nó không nói netmiko thật kết nối được, không nói envelope device đúng flat-dict. Hai cái đó là assume riêng, chỉ phần cứng đóng.

## Open item

- **Live netmiko chưa chạy lần nào (chân trời, cần device).** Assume DUY NHẤT còn lại trong stack. Mock đóng wiring, không đóng nó.
- **Envelope thật trên device (flat dict vs nested dưới `/platform/control[A]`).** Unwrap trong `fetch_control` đang ĐOÁN flat-dict-hoặc-single-list. Nếu device trả nested, cả unwrap lẫn parser sai. Chỉ device verify.
- **Recapture `control_A.json` full-fidelity (nợ Day 20 đến giờ).** Bản hiện trim; đè full output `info from state platform control A` khi pull lại.
- **github remote + push.** Giờ là đòn bẩy kép: binding-record durable + kênh tôi clone tự động để giết paste đầu phiên. Auth là bước Trung. Public hay private quyết định có clone tự động được không.
- **Mirror content-check.** Vẫn nợ refresh, nhưng đã HẠ CẤP khỏi nguồn-sự-thật nên không còn block forward-work. Connector silent-fail là caution thường trực, không phải sự cố một lần.

## Baseline (đếm lại, pending pytest đĩa confirm)

Engine pure-parse list-based (cpu list/index=all/average-1, memory utilization, temperature instant không range-check). Alert một-evaluator scalar, strict `>`, type-guard threshold. Monitor alarm-status authoritative cho temperature (`basis` mỗi metric, `margin` in-metric), threshold fallback, NO_THRESHOLD trung thực. Fetcher CLI seam: parse + error-path + envelope-unwrap validate trên bytes thật qua mock SSH; live SSH chưa. `main.py` entry point đọc capture thật + render inline, smoke test. **Lớp seam `test_pipeline.py` nối fetcher → evaluate → render end-to-end, happy + 6 error-path + envelope, 10 test.** Một real capture làm fixture. Git mốc `3667cf9` (Day 28) → `62edd31` (Day 29) → follow-on Day 30 cho `test_pipeline.py`. **40 green** (30 Day 29 + 10 seam) trên real shape, pending xác nhận bằng `git log -1 && pytest -q` trên đĩa. Mọi Day N+1 đếm từ đây, lấy baseline bằng MỘT lệnh git+pytest, không đọc mirror.
